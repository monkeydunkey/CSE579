The paper  |  outputs  | control
The paper  |  outputs  | spatial location
The paper  |  outputs  | control over spatial location
The paper  |  outputs  | method
The paper  |  outputs  | high-resolution
The paper  |  outputs  | stylisation
The paper  |  outputs  | common failure cases such
The paper  |  outputs  | ground textures
The paper  |  outputs  | regions
The paper  |  outputs  | method by allowing high-resolution controlled stylisation and helps to alleviate common failure cases such as applying ground textures to sky regions
The paper  |  outputs  | artwork such
The paper  |  outputs  | vincent van gogh
The paper  |  outputs  | wheatﬁeld
The paper  |  outputs  | cypresses
The paper  |  outputs  | artwork such as vincent van gogh 's a wheatﬁeld with cypresses
The paper  |  solves  | meaningful control
The paper  |  solves  | recent image stylisation method
The paper  |  solves  | meaningful control to a recent image stylisation method
The paper  |  outputs  | meaningful control
The paper  |  outputs  | recent image stylisation method
The paper  |  outputs  | meaningful control to a recent image stylisation method
The paper  |  outputs  | methods
The paper  |  outputs  | image stylisation
The paper  |  outputs  | different spatial regions
The paper  |  outputs  | fig
The paper  |  outputs  | methods for controlling image stylisation independently in different spatial regions ( fig
The paper  |  outputs  | factorisation
The paper  |  outputs  | style
The paper  |  outputs  | aspects
The paper  |  outputs  | style information
The paper  |  outputs  | multiple images
The paper  |  outputs  | creation
The paper  |  outputs  | interesting styles
The paper  |  outputs  | factorisation of style into these aspects can gracefully combine style information from multiple images and thus enable the creation of new , perceptually interesting styles
The paper  |  uses  | vgg-19 network
The paper  |  uses  | style
The paper  |  outputs  | means
The paper  |  outputs  | control neural style transfer
The paper  |  outputs  | means to spatially control neural style transfer
The paper  |  uses  | style
The paper  |  uses  | sky region
The paper  |  uses  | style to the sky region
The paper  |  outputs  | guidance channels
The paper  |  outputs  | layer
The paper  |  outputs  | guidance channels for each layer
The paper  |  uses  | eroded version
The paper  |  uses  | spatial guiding channels
The paper  |  uses  | eroded version of the spatial guiding channels
The paper  |  uses  | unguided style loss
The paper  |  uses  | neurons
The paper  |  uses  | unguided style loss for neurons
The paper  |  outputs  | simple methods
The paper  |  outputs  | colours
The paper  |  outputs  | source image
The paper  |  outputs  | neural style transfer-in other
The paper  |  outputs  | words
The paper  |  outputs  | style
The paper  |  outputs  | colours
The paper  |  outputs  | simple methods to preserve the colours of the source image during neural style transfer-in other words , to transfer the style without transferring the colours
The paper  |  outputs  | colours
The paper  |  outputs  | source image
The paper  |  outputs  | colours of the source image
The paper  |  uses  | linear methods
The paper  |  uses  | colour style transfer
The paper  |  uses  | linear methods , which are simple and effective for colour style transfer
The paper  |  outputs  | methods
The paper  |  outputs  | different styles
The paper  |  outputs  | different scales
The paper  |  outputs  | high-resolution output
The paper  |  outputs  | style
The paper  |  outputs  | desired scales
The paper  |  outputs  | methods for mixing different styles at different scales , and generating high-resolution output with style at desired scales
The paper  |  outputs  | method
The paper  |  outputs  | stylisation
The paper  |  outputs  | different spatial scales
The paper  |  outputs  | method to control the stylisation independently on different spatial scales
The paper  |  solves  | gram matrices
The paper  |  solves  | different layers
The paper  |  solves  | cnn
The paper  |  solves  | gram matrices from different layers in the cnn
The paper  |  uses  | gram matrices
The paper  |  uses  | different layers
The paper  |  uses  | cnn
The paper  |  uses  | gram matrices from different layers in the cnn
The paper  |  outputs  | new style image
The paper  |  outputs  | ﬁne-scale information
The paper  |  outputs  | image
The paper  |  outputs  | coarse scale information
The paper  |  outputs  | new style image that combines ﬁne-scale information from one image with coarse scale information
The paper  |  uses  | new style image
The paper  |  uses  | neural style transfer
The paper  |  uses  | ﬁne-scale style image
The paper  |  uses  | coarse-scale style image
The paper  |  uses  | neural style transfer from the ﬁne-scale style image to the coarse-scale style image
The paper  |  outputs  | fast neural style transfer methods
The paper  |  uses  | colour control
The paper  |  uses  | fast neural style transfer methods
The paper  |  uses  | colour control described above to these fast neural style transfer methods
The paper  |  uses  | johnson
The paper  |  uses  | excellent publicly-available implementation
The paper  |  uses  | fast neural style transfer
The paper  |  uses  | johnson 's excellent publicly-available implementation of fast neural style transfer
The paper  |  uses  | well-tuned default parameters
The paper  |  uses  | implementation
The paper  |  uses  | instance normalization
The paper  |  uses  | ]
The paper  |  uses  | details
The paper  |  uses  | well-tuned default parameters in that implementation including instance normalization [ 24 ] ( for details
The paper  |  uses  | styles
The paper  |  uses  | implementation
The paper  |  uses  | styles published with that implementation
The paper  |  uses  | order
The paper  |  outputs  | high-resolution output
The paper  |  uses  | initialisation
The paper  |  uses  | neural style
The paper  |  uses  | content/style
The paper  |  uses  | initialisation for neural style ( a ) content/style
The paper  |  uses  | different styles
The paper  |  uses  | different regions
The paper  |  uses  | different styles to different regions
The paper  |  outputs  | feed-forward network
The paper  |  outputs  | different styles
The paper  |  outputs  | different regions
The paper  |  outputs  | feed-forward network to apply different styles to different regions
The paper  |  outputs  | feed-forward network
The paper  |  outputs  | different styles
The paper  |  outputs  | different regions
The paper  |  outputs  | feed-forward network to apply different styles to different regions
The paper  |  outputs  | style image
The paper  |  outputs  | candy
The paper  |  outputs  | feathers images
The paper  |  outputs  | candy and feathers images
The paper  |  uses  | feed-forward network
The paper  |  outputs  | algorithm
The paper  |  outputs  | fast spatially-varying stylisation
The paper  |  outputs  | algorithm that performs fast spatially-varying stylisation
The paper  |  outputs  | intuitive ways
The paper  |  outputs  | neural style transfer
The paper  |  outputs  | intuitive ways to control neural style transfer
The paper  |  outputs  | neural style transfer
The paper  |  outputs  | ways
The paper  |  outputs  | access
The paper  |  outputs  | factors
The paper  |  outputs  | style transfer
The paper  |  outputs  | slow versions
The paper  |  outputs  | ways to access these factors during style transfer , in both fast and slow versions
The paper  |  solves  | image style
The paper  |  solves  | factors
The paper  |  solves  | space
The paper  |  solves  | colour
The paper  |  solves  | scale
The paper  |  solves  | image style includes factors of space , colour , and scale